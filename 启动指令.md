# 启动指令

## 环境

- Node.js 22 或更新版本
- npm
- Windows、macOS 或 Linux X11/XWayland

Linux 原生 Wayland 不支持 Electron 所需的程序化窗口定位，因此完整 `summon(x, y)` 暂不支持原生 Wayland。

## 首次安装

```bash
npm install
```

依赖只安装在本仓库的 `node_modules`，不会写入 npm 全局环境。

## 启动桌宠

```bash
npm start
```

点击奶蛙会随机切换动作；按住奶蛙拖动即可改变位置。

## 调用 summon

统一入口是主进程导出的 `summon(x, y)`。参数是屏幕 DIP 坐标，目标点对应角色脚底中心。preload 同时暴露了同名受限接口 `window.desktopPet.summon(x, y)`，供后续“全局快捷键 → 当前鼠标坐标”直接复用。

## 验证

```bash
npm test
npm run typecheck
npm run build
```
