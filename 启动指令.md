# 启动指令

## 环境

- Node.js 22 或更新版本
- npm
- Windows、macOS 或 Linux X11/XWayland

安装依赖和各系统注意事项见 [README.md](./README.md#安装与运行)。Linux 原生 Wayland 不支持 Electron 所需的程序化窗口定位，因此完整 `summon(x, y)` 暂不支持原生 Wayland；请使用 XWayland。

## 首次安装

```bash
npm install
```

依赖只安装在本仓库的 `node_modules`，不会写入 npm 全局环境。

## 启动桌宠

```bash
npm start
```

点击奶蛙会随机切换动作；按住奶蛙拖动即可改变位置。默认按 `CommandOrControl+Alt+P` 可将桌宠召唤到鼠标位置。

右键奶蛙选择“设置”，可调整桌宠、大小、默认位置和召唤快捷键。配置保存在 Electron `userData/settings.json`。

## 调用 summon

统一入口是主进程导出的 `summon(x, y)`。参数是屏幕 DIP 坐标，目标点对应角色脚底中心。主进程会读取当前真实窗口尺寸，并按鼠标所在屏幕的可用区域限制目标坐标。

## 验证

```bash
npm test
npm run typecheck
npm run build
```
